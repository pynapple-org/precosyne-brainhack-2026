[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "brainhack"
version = "0.1.0"
description = "Brainhack 2026 project"
requires-python = ">=3.10"
dependencies = [
    "nemos",
]

[tool.setuptools.packages.find]
where = ["src"]